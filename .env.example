# jevai-products — copy to .env and fill. NEVER commit .env.

# TypeSafe / Jev API key (https://console.typesafe.ai). Required for live judgments.
TYPESAFE_API_KEY=

# Default model. jev-latest → current jev-1.13.0
JEV_MODEL=jev-latest

# Engine HTTP port
PORT=8080
