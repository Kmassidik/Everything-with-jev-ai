package web

templ Layout(title string) {
	<!DOCTYPE html>
	<html lang="en" class="h-full">
		<head>
			<meta charset="utf-8"/>
			<meta name="viewport" content="width=device-width, initial-scale=1"/>
			<title>{ title }</title>
			<link rel="preconnect" href="https://fonts.googleapis.com"/>
			<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin="anonymous"/>
			<link
				href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=JetBrains+Mono:wght@400;500&family=Space+Grotesk:wght@500;600;700&display=swap"
				rel="stylesheet"
			/>
			<link rel="stylesheet" href={ "/static/css/app.css?v=" + CSSVersion }/>
			<script src="/static/js/htmx.min.js" defer></script>
		</head>
		<body class="min-h-full bg-paper font-sans text-ink">
			{ children... }
		</body>
	</html>
}

templ Nav() {
	<header class="sticky top-0 z-40 border-b border-line bg-paper/85 backdrop-blur">
		<div class="wrap flex items-center gap-6 py-3">
			<a href="/" class="font-display text-lg font-bold tracking-tight">jevai<span class="text-brand">.</span></a>
			<nav class="ml-auto flex items-center gap-6 text-sm">
				<a href="#surfaces" class="text-muted transition hover:text-ink">Products</a>
				<a href="#how" class="text-muted transition hover:text-ink">How</a>
				<a href="#" class="text-muted transition hover:text-ink">Docs</a>
				<a href="#" class="hidden text-muted transition hover:text-ink sm:inline">Sign in</a>
				<a href="#try" class="btn-brand">Get started</a>
			</nav>
		</div>
	</header>
}

templ Foot() {
	<footer class="border-t border-line">
		<div class="wrap flex flex-wrap items-center gap-4 py-8 text-sm text-muted">
			<span class="font-display font-bold text-ink">jevai<span class="text-brand">.</span></span>
			<span>calibrated judgment for every item</span>
			<span class="ml-auto font-mono text-xs">© 2026 · powered by Jev</span>
		</div>
	</footer>
}

// Panel is a clean titled card used by the report/result fragments.
templ Panel(title string) {
	<div class="card overflow-hidden">
		<div class="flex items-center gap-2 border-b border-line bg-paper px-4 py-2.5">
			<span class="dot bg-brand"></span>
			<span class="label">{ title }</span>
		</div>
		<div class="p-5">
			{ children... }
		</div>
	</div>
}
